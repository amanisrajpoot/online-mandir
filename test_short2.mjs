import shortUUID from 'short-uuid'
console.log("shortUUID type:", typeof shortUUID)
console.log("shortUUID keys:", Object.keys(shortUUID))
console.log("shortUUID.default type:", typeof shortUUID.default)
